#include "main.h"
using namespace std;

studentList stuL;

int main(){
    // SetConsoleOutputCP(65001);
    printGreetMenu();
    while(true) printMainMenu();
    // system("pause");
    getchar();getchar();
    return 0;
}